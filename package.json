{
  "name": "loops",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:convex": "npm run dev -w packages/convex",
    "deploy": "npm run deploy -w packages/convex"
  }
}
